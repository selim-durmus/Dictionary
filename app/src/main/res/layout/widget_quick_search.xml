<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget body that adapts gracefully from 1×1 up. The icon is fixed-size and centered;
  the label uses layout_weight=1 + ellipsize so it shrinks to nothing on a 1×1 cell
  (icon still visible) and shows the full text once the widget is wider.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:gravity="center"
    android:orientation="horizontal"
    android:paddingHorizontal="12dp"
    android:paddingVertical="8dp">

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:contentDescription="@string/widget_label"
        android:src="@drawable/ic_search" />

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingStart="10dp"
        android:text="@string/widget_label"
        android:textColor="#D4B062"
        android:textSize="14sp" />
</LinearLayout>
